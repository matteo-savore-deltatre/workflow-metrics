name: Workflow Metrics
author: kittychiu
description: Generate metrics for historical workflow runs
runs:
  using: 'docker'
  image: 'docker://ghcr.io/matteo-savore-deltatre/workflow-metrics:v1'
branding:
  icon: 'rss'
  color: 'orange'